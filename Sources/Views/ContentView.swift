import TokamakDOM

struct ContentView: View {
    var body: some View {

        HStack(alignment: .center){
            Text("Merch")
                .foregroundColor(.white)
            Text("Music")
                .foregroundColor(.white)
            Text("Book Me")
                .foregroundColor(.white) 
        }
       // .foregroundColor(ODTheme.Colors.textPrimary)
    }
}
